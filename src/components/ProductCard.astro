---
interface Props {
    productId: string
}
import { Products, db, eq } from "astro:db"

const { productId } = Astro.props

/* const product = await db.select().from(Products).where(eq(productId, Products.id))
console.log(product) */
/* const prod = product[0]; */

---

<article>
<!-- <img src={prod.imgUrl} alt="Hola"> -->




</article>