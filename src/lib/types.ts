export type Products = {
    id: string
    sku: string
    name: string
    variants: string[]
    category: string[]
    price: number
    stock: number
    imgUrl: string[]
    imgAlt: string[]
  }